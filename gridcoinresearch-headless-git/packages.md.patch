--- a/depends/packages.md	2026-05-20 11:46:52.958205274 -0400
+++ b/depends/packages.md	2026-05-20 11:47:10.808965834 -0400
@@ -57,10 +57,10 @@
 Most variables can be prefixed with the host, architecture, or both, to make
 the modifications specific to that case. For example:
 
-    Universal:     $(package)_cc=gcc
-    Linux only:    $(package)_linux_cc=gcc
-    x86_64 only:       $(package)_x86_64_cc = gcc
-    x86_64 Linux only: $(package)_x86_64_linux_cc = gcc
+    Universal:     $(package)_cc=clang
+    Linux only:    $(package)_linux_cc=clang
+    x86_64 only:       $(package)_x86_64_cc = clang
+    x86_64 Linux only: $(package)_x86_64_linux_cc = clang
 
 These variables may be set to override or append their default values.
 
